import React from "react";

const Title = (props) => {
  return (
    <div
      style={{
        fontSize: "60px",
        textAlign: "center",
        fontWeight: "600",
        letterSpacing: ".2ch",
      }}
    >
      PO Generator
    </div>
  );
};

export default Title;
